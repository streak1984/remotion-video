{
  "mcpServers": {
    "remotion-video": {
      "command": "npx",
      "args": ["tsx", "${CLAUDE_PLUGIN_ROOT}/server/src/index.ts"],
      "cwd": "${CLAUDE_PLUGIN_ROOT}/..",
      "env": {
        "REMOTION_PROJECT_ROOT": "${CLAUDE_PLUGIN_ROOT}/..",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}

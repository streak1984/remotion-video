{
  "mcpServers": {
    "remotion-video": {
      "command": "bash",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/start-server.sh"],
      "env": {
        "REMOTION_PROJECT_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
